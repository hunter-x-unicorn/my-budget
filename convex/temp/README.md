# TEMPORARY — historical NBRB backfill

Delete this entire folder when backfill is complete, plus:

- `src/temp/`
- TEMP block in `src/app/BudgetApp.tsx`
- `src/styles/index.css` import of `temp/backfill.css` (if added)
