/**
 * TEMPORARY — typed access to backfill Convex module.
 */

import { api } from "../../convex/_generated/api";

/** TEMP: `convex/temp/exchangeRatesBackfill.ts` */
export const backfillApi = api.temp.exchangeRatesBackfill;
